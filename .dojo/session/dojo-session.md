# Dojo Session

mode: supervised
rigor: real
type: feature
wave: 3
step: DONE
gate_density: standard
wave_ceiling: 4
intent: Doc-consistency wave — the README's solo-first line stops claiming "one repo" (outgrown by the 2026-09-05 Promoted entry "a wave that spans repositories closes in every repository it touched") and the .dojo/ footprint paragraph acknowledges the per-repo / cross-repo-closure case, so a cold reader hits no contradiction between the two.
goal: README.md says "one human, one agent, one loop"; the .dojo/ paragraph names the per-repo footprint and the cross-repo wave closure (commit, proof, the counterpart's hash); no other file changed; lint + mechanics green; R16 parity green — the landing page carries no solo-first line (grep-verified), so parity is untouched.
commit_style: conventional
test_written: pre-edit census (14 hit-groups swept for "one repo" / "one folder" / wave-close single-repo language across README, docs/index.html, .dojo/ artifacts, all skill files, DOJO-MANUAL, scripts; landing-page solo-first absence = grep exit 1)
test_status: passing ✓
attempts: 0
pre_existing_failures: none
