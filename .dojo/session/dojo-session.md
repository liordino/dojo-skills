# Dojo Session

mode: supervised
rigor: real
type: feature
wave: 3
step: DONE
gate_density: standard
wave_ceiling: 4
intent: where Dojo leans on LLM judgment for a question with a checkable answer, the evidence pass inventories every judge/verify instruction and triages it, so only real-failure-backed candidates become checks and the rest is settled as the model's to answer.
goal: the determinize principle is recorded in dojo-principles → Promoted (local) — terse rule + context-collapse reason, receipt lint R20, provenance (determinize session, 2026-09-10) — proven by 7/7 wave asserts (name, sensor/interpreter split, evidence precondition, receipt, provenance) and a green lint/proof gate.
commit_style: conventional
test_written: wave-3 asserts (.dojo/session/wave-asserts.sh) — Promoted entry: name, sensor/interpreter split, evidence precondition, context-collapse reason, receipt R20, provenance; whitespace-squeezed wrap matching
test_status: passing ✓ (seeded violation: masked junk dir + masked durable file both failed R20 with the offending rule named; restore → green)
attempts: 0
pre_existing_failures: none
